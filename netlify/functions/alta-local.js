
// Minimal placeholder function (your repo likely has one already)
exports.handler = async (event) => {
  return { statusCode: 200, body: JSON.stringify({ ok:true, info:'placeholder function' }) };
};
